name: Build and Release EXE (32-bit and 64-bit)

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      matrix:
        arch: [x86, x64]
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python (${{ matrix.arch }})
        uses: actions/setup-python@v5
        with:
          python-version: '3.8'
          architecture: ${{ matrix.arch }}

      - name: Install dependencies
        run: pip install pyinstaller

      - name: Create UAC manifest
        shell: pwsh
        run: |
          @"
          <?xml version="1.0" encoding="UTF-8" standalone="yes"?>
          <assembly xmlns="urn:schemas-microsoft-com:asm.v1" manifestVersion="1.0">
            <trustInfo xmlns="urn:schemas-microsoft-com:asm.v3">
              <security>
                <requestedPrivileges>
                  <requestedExecutionLevel level="requireAdministrator" uiAccess="false"/>
                </requestedPrivileges>
              </security>
            </trustInfo>
          </assembly>
          "@ | Out-File -FilePath admin.manifest -Encoding utf8

      - name: Build EXE with PyInstaller
        run: |
          pyinstaller --onefile --windowed --manifest admin.manifest --name HotspotController-${{ matrix.arch }} hotspot_controller.py

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: HotspotController-${{ matrix.arch }}
          path: dist/HotspotController-${{ matrix.arch }}.exe

      - name: Create Release and Upload both EXEs (tags only)
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/HotspotController-x86.exe
            dist/HotspotController-x64.exe
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
